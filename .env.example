# .env.example - copy to .env and fill values locally (do NOT commit .env)
DATABASE_URL="postgres://<user>:<pass>@<host>:<port>/<db>?sslmode=require"
HOST="127.0.0.1"
PORT="3001"
APP_ENV="development"
